[package]
name = "margatroid"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tracing = "0.1"
nix = { version = "0.29", features = ["signal", "process", "fs"] }
hostname = "0.4"
libc = "0.2"
